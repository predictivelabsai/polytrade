# CLAUDE.local.md — pointer

All session state, PR queue, and conventions for this repo live in
`AGENTS.md` at the repo root (uncommitted; written for AI coding agents,
primarily Codex). Read that file first — it is the single source of truth.

Design direction: `.impeccable.md` (committed via PR #31).
